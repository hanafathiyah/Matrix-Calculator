package tubes.algeo.lib;

import tubes.algeo.lib.type.Node;
import tubes.algeo.lib.type.LinearFunction;

public class Regresi {
  public static LinearFunction getFunction(Node[] nodes){
    double[] x = {0};
    return new LinearFunction(x);
  }
}
